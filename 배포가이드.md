# CIE-H14A Modbus Controller 배포 가이드

## 배치 파일 목록

프로젝트 루트 디렉토리에 다음 배치 파일들이 제공됩니다:

### 1. `deploy.bat` - 전체 배포 (권장)

**용도**: 처음 배포하거나 전체를 새로 빌드할 때 사용

**실행 내용**:
- Docker 실행 상태 확인
- 기존 컨테이너 중지 및 제거
- Docker 이미지 빌드 (캐시 없이 새로 빌드)
- 컨테이너 시작
- 상태 확인 및 Health Check

**사용 방법**:
```cmd
deploy.bat
```

**소요 시간**: 약 3-5분 (네트워크 상태에 따라 다름)

---

### 2. `start.bat` - 컨테이너 시작

**용도**: 이미 빌드된 이미지로 컨테이너를 시작할 때

**실행 내용**:
- 컨테이너 시작 (백그라운드)
- 상태 확인
- 최근 로그 표시

**사용 방법**:
```cmd
start.bat
```

**소요 시간**: 약 5-10초

---

### 3. `stop.bat` - 컨테이너 중지

**용도**: 실행 중인 컨테이너를 중지할 때

**실행 내용**:
- 모든 컨테이너 중지 (데이터는 유지됨)

**사용 방법**:
```cmd
stop.bat
```

**소요 시간**: 약 3-5초

---

### 4. `restart.bat` - 컨테이너 재시작

**용도**: 설정 변경 후 재시작하거나 문제 해결할 때

**실행 내容**:
- 컨테이너 재시작
- 상태 확인
- 최근 로그 표시

**사용 방법**:
```cmd
restart.bat
```

**소요 시간**: 약 10초

---

### 5. `logs.bat` - 실시간 로그 보기

**용도**: 실시간으로 로그를 모니터링할 때

**실행 내용**:
- 실시간 로그 스트림 표시
- Ctrl+C로 종료 가능

**사용 방법**:
```cmd
logs.bat
```

**종료**: `Ctrl + C`

---

### 6. `status.bat` - 시스템 상태 확인

**용도**: 현재 시스템 상태를 전체적으로 확인할 때

**실행 내용**:
- 컨테이너 상태 확인
- 최근 로그 20줄 표시
- Health Check 수행
- API 상태 확인
- 시스템 모니터링 정보 표시

**사용 방법**:
```cmd
status.bat
```

---

### 7. `clean.bat` - 완전 정리

**용도**: 모든 것을 삭제하고 처음부터 다시 시작할 때

**실행 내용**:
- 모든 컨테이너 중지 및 제거
- 모든 이미지 제거
- 모든 볼륨 제거
- 모든 네트워크 제거
- Docker 시스템 정리

**주의**: 이 작업은 되돌릴 수 없습니다!

**사용 방법**:
```cmd
clean.bat
```

**소요 시간**: 약 30초

---

## 사용 시나리오

### 시나리오 1: 처음 배포

```cmd
1. deploy.bat 실행
2. 브라우저에서 http://localhost:5000 접속
```

### 시나리오 2: .env 설정 변경 후

```cmd
1. .env 파일 수정
2. restart.bat 실행
3. logs.bat으로 로그 확인
```

### 시나리오 3: 코드 수정 후 재배포

```cmd
1. 코드 수정
2. deploy.bat 실행
3. status.bat으로 상태 확인
```

### 시나리오 4: 문제 발생 시

```cmd
1. logs.bat으로 로그 확인
2. status.bat으로 상태 확인
3. restart.bat으로 재시작 시도
4. 해결 안 되면 clean.bat 후 deploy.bat 실행
```

### 시나리오 5: 일상적인 시작/종료

```cmd
# 시작
start.bat

# 종료
stop.bat
```

---

## 주요 URL

배포 완료 후 다음 URL로 접근할 수 있습니다:

| 항목 | URL | 설명 |
|------|-----|------|
| **메인 페이지** | http://localhost:5000 | 웹 대시보드 |
| **API 상태** | http://localhost:5000/api/status | Modbus 연결 및 I/O 상태 |
| **Health Check** | http://localhost:5000/health | 컨테이너 헬스 체크 |
| **모니터링** | http://localhost:5000/api/monitor | API 모니터링 정보 |
| **설정 확인** | http://localhost:5000/api/config | Modbus 설정 확인 |

---

## 문제 해결

### 1. "Docker가 실행 중이 아닙니다" 오류

**해결책**:
- Docker Desktop을 실행하세요
- Docker Desktop이 완전히 시작될 때까지 기다리세요 (약 30초)

### 2. "포트 5000이 이미 사용 중입니다" 오류

**해결책**:
```cmd
# 다른 프로세스 확인
netstat -ano | findstr :5000

# 또는 .env에서 포트 변경
FLASK_PORT=5001
```

### 3. "Modbus 연결 실패" 오류

**해결책**:
- `.env` 파일에서 `MODBUS_HOST` IP 주소 확인
- CIE-H14A 장비가 네트워크에 연결되어 있는지 확인
- ping 테스트: `ping 192.168.10.105`

### 4. 컨테이너가 계속 재시작됨

**해결책**:
```cmd
# 로그 확인
logs.bat

# 완전 정리 후 재배포
clean.bat
deploy.bat
```

---

## Docker 명령어 직접 사용

배치 파일 대신 Docker 명령어를 직접 사용할 수도 있습니다:

```cmd
# 빌드
docker-compose build --no-cache

# 시작 (백그라운드)
docker-compose up -d

# 시작 (포그라운드, 로그 보면서)
docker-compose up

# 중지
docker-compose stop

# 중지 및 제거
docker-compose down

# 로그 보기
docker-compose logs -f

# 상태 확인
docker-compose ps

# 컨테이너 내부 접근
docker exec -it modbus-controller bash
```

---

## 환경 변수 설정

`.env` 파일에서 다음 설정을 변경할 수 있습니다:

```env
# Modbus 장비 설정
MODBUS_HOST=192.168.10.105      # CIE-H14A IP 주소
MODBUS_PORT=502                  # Modbus TCP 포트
MODBUS_UNIT_ID=1                 # Modbus Unit ID
MODBUS_TIMEOUT=0.3               # 타임아웃 (초)

# 폴링 설정
POLL_INTERVAL=0.5                # 폴링 주기 (초)

# DI 감지 시 호출할 URL (선택 사항)
SENSOR_URL=http://localhost:5000/get_sensor
SENSOR_DEVICE_ID=1

# Flask 설정
FLASK_PORT=5000                  # 웹 서버 포트
FLASK_ENV=production             # 환경 (production/development)

# 로깅
LOG_LEVEL=INFO                   # 로그 레벨 (DEBUG/INFO/WARNING/ERROR)
```

설정 변경 후 `restart.bat` 실행하여 적용하세요.

---

## 자동 시작 설정 (선택 사항)

Windows 시작 시 자동으로 실행하려면:

1. `작업 스케줄러` 실행
2. `기본 작업 만들기` 선택
3. 트리거: "컴퓨터를 시작할 때"
4. 작업: "프로그램 시작"
5. 프로그램: `deploy.bat` 전체 경로 입력
6. 완료

---

## 백업 및 복구

### 백업

중요한 파일들:
- `.env` - 환경 설정
- `app/` - 애플리케이션 코드
- `config/` - 설정 파일

### 복구

```cmd
1. 백업 파일을 프로젝트 디렉토리에 복사
2. deploy.bat 실행
```

---

## 성능 모니터링

시스템 리소스 확인:

```cmd
# Docker 통계
docker stats modbus-controller

# 컨테이너 리소스 사용량
docker-compose top
```

---

## 지원

문제가 발생하면:

1. `logs.bat`으로 로그 확인
2. `status.bat`으로 상태 확인
3. GitHub Issues에 문의
4. CLAUDE.md 파일 참조

---

## 버전 관리

현재 버전: **v1.0.0**

버전 확인:
```cmd
git tag
git log --oneline
```

새 버전 태그:
```cmd
git tag v1.1.0
git push origin v1.1.0
```
